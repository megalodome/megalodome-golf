"use client";

// PostHog is initialized in instrumentation-client.ts (Next.js 15.3+ recommended approach).
// This component is kept as a no-op so the import in layout.tsx remains valid.
export function PostHogProvider() {
  return null;
}
